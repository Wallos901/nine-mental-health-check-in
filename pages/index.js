import Layout from "../components/layout/Layout";

const Home = () => {
    return (
        <Layout
            title="Nine Mental Health Check In"
            description="Discover the ins and outs of working at Nine with this fun and interactive learning tool."
        />
    );
};

export default Home;
